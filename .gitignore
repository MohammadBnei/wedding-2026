node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Guest list export — real names and personal notes. Never commit this.
*.csv

# The original design artifact (945KB, kept locally as the visual reference).
*wedding site.html

# The licensed Vecteezy stock drawing the arch was traced from. Kept locally as
# the reference $lib/arch.js points at — this repo is public and the licence is
# not ours to redistribute.
vecteezy_*.svg

# Playwright
/test-results/
/playwright-report/
/blob-report/
